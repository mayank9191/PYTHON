# Replace the double space from problem 3 with single spaces.

name = " mayank is a good  boy"

print(name.find("  "))
print(name)  # strings are immutable no function can change them

print(name.replace("  ", " "))
