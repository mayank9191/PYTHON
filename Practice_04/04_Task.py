# Write a program to sum a list with 4 numbers.

lists = [4, 5, 78, 100]

# a = 0

# a = lists.pop(0)
# a = lists.pop(0) + a
# a = lists.pop(0) + a
# a = lists.pop(0) + a

# print("Sum of list : ", a)

print("Sum :", sum(lists))
